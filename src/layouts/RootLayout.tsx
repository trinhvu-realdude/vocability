import { StorageBar } from "../components/StorageBar";

const RootLayout = () => {
    return (
        <div className="container my-4">
            <StorageBar />
            <div className="text-center">Root Layout</div>
        </div>
    );
};

export default RootLayout;
